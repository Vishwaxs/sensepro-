/** Barrel re-export: modules that import from "@/lib/types" resolve here.
 *  The canonical definitions live in "@/lib/data/types". */
export type {
  AttendanceState,
  PresenceState,
  Zone,
  Student,
  RosterEntry,
  Session,
  ProctorFlagType,
  ProctorFlag,
  ZoneAggregate,
  ConsentRecord,
  AuditEntry,
  DeviceRow,
  UserRow,
  AttendanceRecord,
} from "@/lib/data/types";
