# Session Summary — 2026-07-14
**Focus:** Phase 2 complete — persistence, real auth, live Realtime roster, PDF export, council review.

## Done
- Live Supabase (project bwjrnkledjjpcvnzykqc): migrations 0001–0006 applied; 53 real MCA-4B
  students seeded; 5 staff auth users created (4 teachers + 1 management) with `user_roles`
  links; Realtime enabled on `presence_intervals`; service_role + authenticated grants fixed
  (grants and RLS are separate layers — both roles were locked out until 0004/0005).
- Backend write-path (`app/store.py`, `sessions.py`, `ws.py`): open-interval INSERT with
  client uuid + close via PATCH (a council-caught double-insert bug), writes off the event loop
  via `run_in_threadpool`, log-and-drop on failure, NoopWriter offline default. `POST
  /v1/sessions` + `/{id}/end` per the frozen contract. 17/17 tests incl. a WS-loop integration
  test asserting the exact upsert sequence.
- Frontend: real Supabase Auth (`lib/supabase.ts`, `lib/auth.tsx` decodes `app_role` from the
  JWT per Supabase's custom-claims docs), role route-guards (`RequireRole`), live teacher
  roster via direct RLS reads + Realtime subscription with stale-watermark/reconnect, session
  PDF export (jspdf, dynamic import), student self-view under RLS self-read policies,
  delete-my-data kept as a logged stub.
- Council review (4 advisors + peer review, subagents): 7 findings fixed — double-insert,
  blocking httpx, retry-buffer removal (~40 lines), dual key names, CORS `*` default, committed
  demo-password fallback, dead `service_role` JWT check in the proctor trigger (migration 0006).

## Decisions (also append to docs/adr/ if architectural)
- ADR 0006: realtime read path; grants-follow-features (dormant RLS on sensitive tables is
  deliberate); log-and-drop over retry queue; client-side PDF.
- Access Token Hook delivers `app_role` (0003); enabling the hook is a dashboard-only toggle.

## Open / next
- HUMAN: enable the hook (Auth > Hooks > Customize Access Token → `custom_access_token_hook`)
  — logins work but carry no role until then; commit Phase-2 work (sequential plan provided);
  rotate the sb_secret_/anon keys (pasted in chat earlier — treat as burned); decide whether
  real student names in `mock.ts`/git are acceptable for a private repo.
- Link student logins to `students.auth_uid` when student accounts are issued (self-view is
  empty until then). Wire Management/Admin dashboards to live data (Phase 3).
- End-to-end demo run: sign in → create session → capture attaches session_id → roster fills
  live in a second tab → export PDF.

## Invariant check
- [x] no training added  - [x] no raw frames stored  - [x] no per-student engagement  - [x] tests green
