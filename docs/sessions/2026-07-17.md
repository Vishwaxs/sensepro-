# Session Summary — 2026-07-17
**Focus:** Phase 3 — RTSP camera capture, proctor mode with gaze-down suppression, VNEI zone
engagement, live review/analytics UI, eval harness. Branch: feat/phase-3-camera-proctor-vnei.

## Done
- `capture/`: RtspSource (latest-frame-wins, reconnect with backoff, password always masked)
  + `python -m capture.run_session --rtsp ... --session <id> --mode lecture|exam`. Same
  SessionPipeline + SessionRecorder as the WS path; observers run proctor/engagement at the
  sampled rate. `--enrolled front=18,...` (even roster split by default) feeds coverage.
- `proctor/`: ObjectDetector protocol — stub (colour markers) / YOLOv8n pretrained behind a
  lazy import (`.[proctor]`, PROCTOR_BACKEND=yolo). GazeSuppressor + landmark pitch heuristic
  (eye line ~40% of box = level). Engine: phone → nearest track within 1.5 diagonals →
  suppressed if that track wrote recently → per-key cooldown → proctor_flags row. Schema
  facts honoured: review_status enum is pending/dismissed/upheld ("pending" renders as
  "awaiting review"); no confidence column.
- `engagement/`: transient TrackSignals (attending/head-down, phone-nearby, stillness; EAR
  honestly omitted — kps have no eyelids) → ZoneAggregator (fixed frame bands, 60s windows,
  vnei over visible-track observations, coverage declared, k>=5 suppressed in code AND by the
  DB CHECK). No student identifier anywhere on engagement records — pinned by test.
- `eval/`: `python -m eval.run --clip x.mp4 --truth truth.json --mode exam` → recognition
  hit-rate, presence-duration error, proctor FP with the gaze filter ON vs OFF (+CSV).
  Synthetic test proves the measurement: fp_reduction 100% on the scripted scenario.
- Frontend: Teacher gets a live Proctor review panel (Realtime, Dismiss/Uphold writing
  reviewer+time, "Awaiting review" language); Management VNEI view is live (zone bars +
  coverage badges, <50% hatched LOW CONFIDENCE, missing windows shown "suppressed (k<5)",
  per-zone sparkline trend). Mock data removed from both routes.
- ADRs 0007 (VNEI aggregation, EU AI Act red line) and 0008 (capture-source abstraction).
- Backend 43/43, ruff clean; web build green (tsc strict + vite).

## Incident (recorded so it isn't mysterious later)
Mid-session the working tree switched to main and pulled "learn-eye golden master" (a
wholesale apps/web replacement) — Phase 1+2 work was safe on feat/phase-1-foundations
(committed + pushed). Phase 3 branched from there; learn-eye reconciliation is a pending PR
decision (it deleted web/capture.html and replaced our frontend wholesale). node_modules
needed a reinstall after switching back.

## Open / next
- HUMAN: apply supabase/migrations/0007_phase3_read_paths.sql (grants + realtime for
  proctor_flags/engagement_zone_aggregates) — the auto-mode classifier requires the user to
  authorize this specific live-DB permission change; panels read empty until then. Enable the
  access-token hook (Auth > Hooks) — still pending from Phase 2; rotate the burned keys.
- HUMAN: commit Phase 3 (suggested: feat(capture), feat(proctor), feat(engagement),
  feat(capture) wiring, feat(db) 0007, feat(web), test/docs) and the VLC gate + live camera
  run; record the honest 2/4/6 m coverage numbers via the eval harness.
- Decide the learn-eye merge story before the demo.

## Invariant check
- [x] no training  - [x] no raw frames stored  - [x] no per-student engagement (test-pinned)
- [x] no emotion labels  - [x] flags are review-only  - [x] tests green
